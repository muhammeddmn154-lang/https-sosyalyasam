# linkora/client

Bu klasör bir React istemcisi içindir. Başlangıç için aşağıdaki komutlarla bir proje başlatabilirsiniz:

```bash
npx create-react-app .
# veya
pnpm create vite@latest . --template react
```
